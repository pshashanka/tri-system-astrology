{
  "private": true,
  "scripts": {
    "dev": "tsx watch server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "mcp": "npx tsx mcp/server.ts",
    "plugin:zip": "rm -rf .pluginpkg triad-astro-plugin.zip && mkdir -p .pluginpkg && cp -R plugin .pluginpkg/triad-astro && (cd .pluginpkg && zip -r -X ../triad-astro-plugin.zip triad-astro >/dev/null) && rm -rf .pluginpkg && unzip -l triad-astro-plugin.zip | grep -E '\\.mcp\\.json|plugin\\.json'"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.4",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.37.0",
    "astronomy-engine": "^2.1.19",
    "hono": "^4.9.8",
    "lunar-javascript": "^1.7.7",
    "openai": "^6.33.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
